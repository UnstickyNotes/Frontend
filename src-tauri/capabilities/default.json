{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier":"core:app:allow-identifier",
      "allow": [
        { "url": "http://localhost:8000/*" },
        { "url": "http://127.0.0.1:8000/*" }
      ]
    }
  ]
}